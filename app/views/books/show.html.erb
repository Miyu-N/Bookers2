<h2>User info</h2>
<%= form_with model: @book, local:true do |f| %>

<h5>name</h5>
<%= @user.name%>

<h5>introduction</h5>
<%= @user.introduction %>
<%= link_to "",edit_user_path(@user.id) %>

<h2>New book</h2>
<h4>Title</h4>
<%= f.text_field :title %>

<h4>Opinion</h4>
<%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>

<h2>Book details</h2>
<table>
  <tr>
    <td>
      <%= attachment_image_tag @book.user %>
        <%= link_to user_path(@boook.user_id) do %>
          <nav>
            <%= @book.user.name %>
          </nav>
    </td>
    
    <td><%= link_to @book.title %></td>
    <td><%= @book.body %></td>
    <% if @book.user_id == current_user.id %>
    <td><%= link_to edit_book_path(@book.id)%></td>
    <td><%= link_to book_path(@book.id), method: :delete%></td>
    <% end %>
  </tr>
</table>